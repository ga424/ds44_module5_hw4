# C4 Documentation - Hadoop WordCount Application

This document provides C4 architecture diagrams for the Java WordCount MapReduce application and its local validation tooling.

## Level 1: System Context

```mermaid
C4Context
  title System Context - Hadoop WordCount Application

  Person(student, "Student/Operator", "Builds and runs WordCount job")
  System(wordcount, "WordCount Application", "Java MapReduce program packaged as a Maven-built JAR")
  System_Ext(hdfs, "HDFS", "Distributed storage for input and output data")
  System_Ext(yarn, "YARN", "Cluster resource manager and job scheduler")

  Rel(student, wordcount, "Builds, submits, and monitors job")
  Rel(wordcount, hdfs, "Reads input and writes output")
  Rel(wordcount, yarn, "Submits and executes MapReduce job")
```

## Level 2: Container Diagram

```mermaid
C4Container
  title Container Diagram - Hadoop WordCount Application

  Person(student, "Student/Operator", "Builds, tests, and runs the job")

  System_Boundary(s1, "WordCount Solution") {
    Container(source, "WordCount.java", "Java", "Mapper, Reducer, Driver, and design-pattern seams")
    Container(benchmark, "WordCountBenchmark.java", "Java/JMH", "Benchmarks normalization and reducer aggregation hot paths")
    Container(buildscript, "mvnw", "Bash", "Compiles, tests, verifies coverage, and creates the JAR")
    Container(runScript, "run_wordcount.sh", "Bash", "Submits Hadoop job and handles output path")
    Container(localValidation, "run_local_containerized.sh", "Bash", "Runs Docker Compose Hadoop smoke tests")
    Container(jar, "target/wordcount-hadoop-1.0.0.jar", "JAR", "Executable MapReduce artifact")
    Container(input, "input.txt", "Text File", "Input text data")
    Container(output, "part-r-00000", "Text File", "Word count result generated in HDFS")
  }

  System_Ext(hdfs, "HDFS", "Distributed file system")
  System_Ext(yarn, "YARN", "Resource manager and scheduler")

  Rel(student, buildscript, "Executes")
  Rel(student, runScript, "Executes")
  Rel(student, localValidation, "Executes")
  Rel(buildscript, source, "Compiles")
  Rel(buildscript, jar, "Builds")
  Rel(buildscript, benchmark, "Compiles")
  Rel(runScript, jar, "Runs with hadoop jar")
  Rel(runScript, input, "Uploads to HDFS")
  Rel(runScript, hdfs, "Creates/deletes paths")
  Rel(localValidation, jar, "Runs with hadoop jar")
  Rel(localValidation, input, "Uploads to HDFS")
  Rel(localValidation, hdfs, "Creates/deletes paths")
  Rel(jar, hdfs, "Reads input and writes output")
  Rel(jar, yarn, "Runs MapReduce tasks")
  Rel(hdfs, output, "Stores result")
```

## Level 3: Component Diagram

```mermaid
C4Component
  title Component Diagram - WordCount JAR

  Person(student, "Student/Operator", "Runs hadoop jar command")

  Container_Boundary(c1, "wordcount-hadoop-1.0.0.jar") {
    Component(driver, "WordCount main", "Java", "Configures and launches Hadoop Job")
    Component(strategy, "TextNormalizationStrategy", "Java", "Allows the mapper to swap text normalization rules")
    Component(mapper, "TokenizerMapper", "Java", "Normalizes input, tokenizes lines, and emits (word,1)")
    Component(jobFactory, "WordCountJobFactory", "Java", "Creates configured Hadoop Job instances")
    Component(combiner, "IntSumReducer as Combiner", "Java", "Locally aggregates mapper output")
    Component(reducer, "IntSumReducer as Reducer", "Java", "Aggregates final word counts")
  }

  Component_Ext(hdfsInput, "HDFS Input Split", "HDFS", "Input data blocks for map tasks")
  Component_Ext(hdfsOutput, "HDFS Output", "HDFS", "Final part files")

  Rel(student, driver, "Invokes")
  Rel(driver, jobFactory, "Uses")
  Rel(driver, strategy, "Uses")
  Rel(driver, mapper, "Sets mapper class")
  Rel(driver, combiner, "Sets combiner class")
  Rel(driver, reducer, "Sets reducer class")
  Rel(strategy, mapper, "Normalizes text")
  Rel(mapper, combiner, "Emits intermediate key-value pairs")
  Rel(combiner, reducer, "Sends aggregated intermediate data")
  Rel(hdfsInput, mapper, "Provides text records")
  Rel(reducer, hdfsOutput, "Writes final counts")
```

## Level 4: Deployment Diagram

```mermaid
flowchart TB
  %% 4-Node Hadoop Cluster deployment (renderable Mermaid)
  subgraph cluster["4-Node Hadoop Cluster (Hadoop runtime)"]
    subgraph master["NameNode (master) — Linux VM\nHDFS NameNode · YARN ResourceManager · client tools"]
      cli[Hadoop Client Shell<br/>Bash<br/>(runs job commands)]
      jar[wordcount-hadoop-1.0.0.jar<br/>JAR (packaged app)]
    end

    subgraph s1["slave1 — Linux VM\nDataNode + NodeManager"]
      map1[Map Task Executor<br/>JVM]
      red1[Reduce Task Executor<br/>JVM]
    end

    subgraph s2["slave2 — Linux VM\nDataNode + NodeManager"]
      map2[Map Task Executor<br/>JVM]
      red2[Reduce Task Executor<br/>JVM]
    end

    subgraph s3["slave3 — Linux VM\nDataNode + NodeManager"]
      map3[Map Task Executor<br/>JVM]
      red3[Reduce Task Executor<br/>JVM]
    end
  end

  devbox["Developer Workstation<br/>macOS / Linux"]
  mvnw[\"mvnw / build scripts\"<br/>(build, test, package)]
  bench[run_benchmarks.sh<br/>JMH bench harness]
  localTest[run_local_containerized.sh<br/>Docker Compose smoke test]

  hdfs["HDFS (distributed storage)\nNameNode / DataNodes"]
  yarn["YARN (cluster services)\nResourceManager / NodeManagers"]

  %% Relationships
  devbox -->|Runs| mvnw
  devbox -->|Runs| bench
  devbox -->|Runs| localTest

  cli -->|Launches| jar
  jar -->|Submits application| yarn

  yarn -->|Schedules containers| map1
  yarn --> map2
  yarn --> map3

  map1 -->|Reads input blocks| hdfs
  map2 -->|Reads input blocks| hdfs
  map3 -->|Reads input blocks| hdfs

  red1 -->|Writes output| hdfs
  red2 -->|Writes output| hdfs
  red3 -->|Writes output| hdfs
```
